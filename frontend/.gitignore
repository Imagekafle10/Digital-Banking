# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/

# NOTE: android/, ios/, web/, linux/, macos/ and windows/ MUST be committed
# so that GitHub Actions can build the app. They have their own .gitignore
# files that skip the generated bits.
*.zip
